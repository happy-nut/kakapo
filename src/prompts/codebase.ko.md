이 저장소를 읽고 노트를 딱 하나 남기세요. 무엇으로 이루어져 있는지에 대한 지도입니다. 저는 방금 합류했고 세부보다 전체 형태가 먼저 필요합니다.

위에서 멈추세요. 이 프롬프트는 파일도, 함수도, 결정도 설명하지 않습니다. 그건 "diff 설명"이 나중에 합니다 - 읽는 사람이 지도의 어느 부분에 서 있는지 알고 난 뒤에요. 지도 하나가 일의 전부이고, 두 번째 노트는 범위를 넘는 것입니다.

노트는 다음 파일에 한 줄에 JSON 하나씩 append 하세요(없으면 상위 디렉터리까지 만드세요. 파일 맨 위 # 헤더에 형식이 적혀 있습니다):
{{NOTES_PATH}}

{"id":<파일에서 가장 큰 id + 1>,"by":"agent","kind":"note","path":"repo/기준/진입점.ts","line":1,"title":"이 저장소는 무엇으로 이루어져 있나","text":"markdown"}
- 오직 append. 리뷰어의 코멘트도, 당신이 앞서 쓴 답변도 같은 파일에 있습니다. 이미 있는 줄은 고치거나 순서를 바꾸거나 번호를 다시 매기지 마세요.
- "text"는 한 줄짜리 JSON 문자열이므로 줄바꿈은 \n으로 씁니다.
- 노트는 진입점에 붙이세요. 가장 먼저 실행되는 곳이 새로 온 사람이 읽기 시작하는 자리이고, 지도는 거기 있어야 합니다.

지도는 두 부분으로 이루어지고, 둘 다 필수입니다.

1. Mermaid 다이어그램. 이 저장소가 실제로 무엇으로 만들어졌는지 핵심 컴포넌트 3~5개를 고르세요. 모든 디렉터리가 아니라, 그것 없이는 시스템을 설명할 수 없는 몇 개입니다.
   - 각 노드는 컴포넌트 이름과 역할을 몇 단어로 함께 적어, 다이어그램만 봐도 읽히게
   - 간선은 무엇이 어느 방향으로 흐르는지 말하게 - 관계가 핵심입니다
   - 모든 노드는 그 컴포넌트가 사는 위치로 링크를 겁니다:
       click A "#kakapo:src/app-main.ts:1198"
     kakapo가 이것을 이동으로 바꿉니다. 노드를 클릭하면 그 파일의 그 줄이 열립니다.

2. 다이어그램 아래에 컴포넌트마다 짧은 문단 하나씩 - 3~4문장, 다이어그램과 같은 순서로. 각 문단은 다음 세 가지를 모두 담아야 합니다:
   - 이 컴포넌트가 책임지는 것, 그리고 일부러 책임지지 않는 것.
   - 예시. 실제로 이곳을 지나가는 구체적인 것 하나를 이름으로 부르세요. "리뷰 요청을 처리한다"는 예시가 아닙니다. "`kakapo:get-file`이 파일 index를 들고 들어와 HTML이 되어 나간다"가 예시입니다. 문단마다 실제 식별자, 메시지 이름, 경로, 숫자가 최소 하나는 들어가야 합니다. 사례에 발을 딛지 못한 추상은 읽고 고개는 끄덕여지지만 기억에 남지 않습니다.
   - 다음으로 가는 링크. 파일 참조는 줄 번호를 붙여 인라인 코드로 쓰세요 - `src/app-review-ipc.ts:50` - kakapo가 그것을 클릭하면 이동하는 링크로 바꿉니다. 이 컴포넌트가 다음 컴포넌트와 맞닿는 이음매를 이름으로 부르고 링크하세요. 두 컴포넌트가 맞닿으면 양쪽 문단이 각자의 자리에서 같은 지점을 가리켜야, 설명이 목록이 아니라 그물이 됩니다.

쓰는 방법:
- 프로그래밍은 할 줄 아는 똑똑한 12살에게 설명하듯 쓰세요. 짧은 문장, 쉬운 단어.
- 용어는 진짜 이름 그대로 쓰되, 처음 나올 때 반 문장으로 값을 치르세요. 이 저장소가 만들어낸 이름은 다릅니다 - pin, manifest, 워크스페이스 같은 말은 그게 무엇인지 말하기 전까지 아무 의미가 없으니, 처음 쓰는 자리에서 정의하세요.
- 추상보다 구체, 언제나. 실제 파일·함수·증상을 이름으로 부르세요. "동기화 이슈"가 아니라 "인덱스를 다시 만드는 동안 창이 1초쯤 비어 보인다"처럼.
- 파일 이름이 무엇인지가 아니라, 그 컴포넌트가 왜 존재하는지로 시작하세요.
- 코드만으로 이유를 알 수 없으면 주석과 커밋 히스토리를 읽으세요.
- 확인하지 못한 것은 쓰지 마세요. 밝혀내지 못했으면 아예 빼세요 - 얼버무리지 말고, "아마", "~인 듯"이라고 쓰지 마세요. 빠진 자리는 "아직 설명 안 됨"으로 읽히지만, 얼버무린 문장은 진짜 문장과 똑같은 자리를 차지하면서 읽는 사람에게 아무 쓸모가 없습니다.
- 노트 전체가 화면 한두 개에 들어가야 합니다. 그보다 길면 컴포넌트를 지도로 그리는 게 아니라 설명하고 있는 것이고, 그건 다른 프롬프트의 일입니다.

파일을 저장한 뒤에는 멈추세요 - kakapo가 자동으로 감지해 코드 위에 지도를 렌더링합니다.
